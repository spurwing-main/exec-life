# This repo is PUBLIC — the live Webflow tag loads loader.js from jsDelivr by
# commit SHA, so it can't be private. It holds the JS that ships to the browser
# and nothing else.
#
# Everything else goes in /local/. One rule, so there is nothing to decide and
# nothing to add here later. See local/README.md.
/local/

# Machine junk
node_modules/
.pnpm-store/
.env.local
.DS_Store
